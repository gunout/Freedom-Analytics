name: 📊 Mise à jour Freedom Radio

on:
  schedule:
    # Exécution tous les lundis à 9h (heure de La Réunion)
    - cron: '0 3 * * 1'  # 3h UTC = 7h à La Réunion
  workflow_dispatch:  # Permet l'exécution manuelle

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: 🔄 Run scraper
        run: python scraper_freedom.py
        continue-on-error: true

      - name: ✅ Vérifier les changements
        id: check
        run: |
          if git diff --quiet data.json; then
            echo "📊 Aucune nouvelle donnée"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "📊 Nouvelles données détectées"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: 📝 Commit et push
        if: steps.check.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Freedom Bot"
          git add data.json
          git commit -m "📊 Mise à jour hebdomadaire - $(date +'%d/%m/%Y')"
          git push